# 04. 단계별 개발 계획

전체 기능을 한 번에 만들지 않고 아래 순서대로 구현한다.
각 단계는 "완료 조건"을 만족하면 끝나며, 끝날 때마다 Git에 커밋하고
해당 폴더의 README.md와 [INDEX.md](INDEX.md)를 갱신한다.

| 단계 | 이름 | 상태 |
|---|---|---|
| 0 | 계획 수립, 문서 작성, 폴더 골격 | 완료 (2026-09-05) |
| 1 | 출발 위치(발사장) 선택 | 예정 |
| 2 | 목적지(천체) 선택 | 예정 |
| 3 | 속도 슬라이더와 로런츠 인자 표시 | 예정 |
| 4 | 시간 지연 계산 엔진 (등속, 편도/왕복) | 예정 |
| 5 | 결과 수치 표 | 예정 |
| 6 | 스톱워치 애니메이션 | 예정 |
| 7 | 생물 수명 비교 (막대 그래프, 생존 아이콘) | 예정 |
| 8 | 가속-감속 모델 추가 | 예정 (후순위) |
| 9 | 검증, 정리, 마무리 | 예정 |

## 0단계: 계획 수립 (완료)

- 요구사항 정리, 물리 근거 문서, 폴더 골격, Git 초기화

## 1단계: 출발 위치(발사장) 선택

**만드는 것**
- `index.html` 진입점
- `src/data/launchSites.js`: 발사장 5곳의 이름, 국가, 위도, 경도
- `src/ui/launchSiteSelector.js`: 발사장 선택 메뉴, 선택한 발사장 정보 표시
- `src/styles/main.css`: 기본 화면 스타일

**완료 조건**
- 브라우저에서 열었을 때 발사장 5곳이 목록에 보인다.
- 하나를 선택하면 이름, 국가, 위도, 경도가 화면에 표시된다.

**시작 전 확인할 것**
- 스크립트 로딩 방식 ([06_decisions.md](06_decisions.md) 미결 질문 1번)

## 2단계: 목적지(천체) 선택

**만드는 것**
- `src/data/destinations.js`: 천체 12개의 이름, 구분, 지구로부터의 평균 거리(m), 출처
- `src/data/constants.js`: c, AU, ly 등 물리 상수
- `src/utils/units.js`: km / AU / 광년 ↔ m 변환
- `src/ui/destinationSelector.js`: 구분별로 묶인 천체 선택 메뉴, 선택한 천체의 거리 표시

**완료 조건**
- 천체 12개가 구분(태양계 / 근처 별 / 은하 규모 / 은하 밖)별로 보인다.
- 선택하면 거리가 적절한 단위(km, AU, 광년)로 표시된다.

## 3단계: 속도 슬라이더와 로런츠 인자 표시

**만드는 것**
- `src/data/rockets.js`: 하한 기준값(팔콘 헤비 지구 탈출 속도 11.2 km/s)과 출처
- `src/physics/lorentz.js`: β, γ 계산 (낮은 속도용 급수 포함)
- `src/ui/speedSlider.js`: 속도 슬라이더(11.2 km/s ~ 0.99c), 현재 속도를 km/s와 광속 비율로 표시
- `src/ui/lorentzDisplay.js`: 속도에 대응하는 로런츠 인자를 슬라이더 형태로 표시

**완료 조건**
- 슬라이더를 움직이면 속도와 γ가 즉시 갱신된다.
- [03_physics.md](03_physics.md) 5절의 기준값과 일치한다.

**시작 전 확인할 것**
- 슬라이더 눈금 방식 (미결 질문 2번)
- 로런츠 인자 슬라이더를 조작 가능하게 할지 (미결 질문 3번)

## 4단계: 시간 지연 계산 엔진

**만드는 것**
- `src/physics/timeDilation.js`: 거리, 속도, 편도/왕복을 받아 지구 시간, 우주선 시간, 차이를 초 단위로 반환
- `src/ui/tripTypeSelector.js`: 편도 / 왕복 선택
- `tests/physics.test.html`: 기준값 검증 페이지

**완료 조건**
- 검증 페이지의 모든 기준값이 통과한다.

## 5단계: 결과 수치 표

**만드는 것**
- `src/utils/formatTime.js`: 초 → 년/일/시/분/초 변환과 큰 수 서식
- `src/ui/resultTable.js`: 지구 시간 / 우주선 시간 / 차이 표

**완료 조건**
- 발사장, 목적지, 속도, 편도/왕복을 바꾸면 표가 즉시 갱신된다.

## 6단계: 스톱워치 애니메이션

**만드는 것**
- `src/ui/stopwatch.js`: `0년 0일 0시 0분`에서 결과값까지 올라가는 애니메이션. 지구 시간과 우주선 시간 두 개를 나란히 표시

**완료 조건**
- 계산 버튼(또는 입력 변경) 후 두 스톱워치가 각자 결과값까지 올라가고 멈춘다.

**시작 전 확인할 것**
- 애니메이션 지속 시간과 시작 방식 (미결 질문 4번)

## 7단계: 생물 수명 비교

**만드는 것**
- `src/data/organisms.js`: 생물 5종의 이름, 대표 수명(초), 출처
- `src/ui/lifespanChart.js`: 생물별 수명 막대 위에 지구 시간과 우주선 시간을 겹쳐 그림 (Canvas 또는 순수 HTML/CSS)
- `src/ui/survivalIcons.js`: 생물별 우주선 탑승 시 / 지구 잔류 시 도착 시점 생존 여부 아이콘

**완료 조건**
- 5종 모두에 대해 그래프와 아이콘이 표시되고, 입력을 바꾸면 갱신된다.

**시작 전 확인할 것**
- 출발 시점의 생물 나이 가정 (미결 질문 5번)
- 그래프 그리는 방식 (미결 질문 6번)

## 8단계: 가속-감속 모델 (후순위)

**만드는 것**
- `src/physics/acceleratedMotion.js`: [03_physics.md](03_physics.md) 4절의 공식
- `src/ui/motionModelSelector.js`: 등속 / 가속-감속 선택
- 가속도 입력 UI

**시작 전 확인할 것**
- 가속도 입력 범위와 방식 (미결 질문 7번)

## 9단계: 검증, 정리, 마무리

- 모든 단계의 완료 조건 재확인
- 모든 폴더 README.md와 INDEX.md 최종 갱신
- 브라우저별(Chrome, Edge) 동작 확인
