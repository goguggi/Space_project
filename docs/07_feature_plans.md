# 07. 기능 계획 누적 기록

새 기능을 계획할 때마다 이 파일에 **하나씩 순서대로 추가**한다.
기존 문서(01~06)는 그대로 두고, 이 파일은 "언제 어떤 기능을 어떻게 만들기로 했는지"의 기록이 된다.
각 기능 항목은 아래 틀을 따른다.

```
## 기능 N. 이름 (계획일)
- 요청 내용 (사용자가 말한 그대로의 요지)
- 확정된 결정
- 만드는 것 (파일 단위)
- 물리/계산 근거 (있을 때)
- 단계 분할과 완료 조건
- 미결 질문
- 다른 문서에 반영한 곳
```

---

## 기능 0. 시간 지연 계산기 핵심 (2026-09-05)

이 프로젝트의 출발점이 된 기능이다. 이미 [01_requirements.md](01_requirements.md)와 [04_roadmap.md](04_roadmap.md)에 정리되어 있으므로 여기서는 요지만 남긴다.

- 발사장 선택 → 천체 12개 중 목적지 선택 → 속도 슬라이더(11.2 km/s ~ 0.99c) → 편도/왕복 → 시간 지연 계산
- 결과: 스톱워치 애니메이션, 수치 표, 생물 5종 수명 대비 막대 그래프, 생존 아이콘
- 개발 순서: [04_roadmap.md](04_roadmap.md) 1~9단계
- 2026-09-05 추가 결정: 로그 눈금 슬라이더, 프리셋 버튼 6개, γ 표시 전용, 스톱워치 자동 시작 3~15초, 년/일/시/분/초 표시,
  생물별 출발 나이 입력, div 막대 그래프, 가속 모델 1g 고정, GitHub Pages 게시 (D-27 ~ D-38, D-46, D-47)

---

## 기능 1. 3D 발사 시뮬레이션 (2026-09-05)

### 요청 내용

- 발사 시 KSP(Kerbal Space Program)처럼 **3인칭 시점에서 로켓 발사를 지켜보고, 카메라가 로켓을 따라간다.**
- 로켓은 **단 분리**를 하며, 분리된 단은 **스페이스X처럼 다시 착륙**한다.

### 확정된 결정

| 번호 | 항목 | 결정 |
|---|---|---|
| D-19 | 표현 방식 | 3D |
| D-20 | 3D 라이브러리 | Three.js 허용. 파일을 `lib/three/`에 동봉하여 인터넷 없이 실행. (D-17 수정: "그래프는 직접 그리고, 3D만 Three.js") |
| D-21 | 로켓 움직임 | 단순 물리 시뮬레이션: 추력, 중력, 연료 소모에 따른 질량 변화. 공기 저항은 생략 |
| D-22 | 로켓 구성 | 팔콘 헤비형: 측면 부스터 2기 + 중앙 1단(코어) + 2단. 분리 순서는 측면 부스터 → 중앙 1단 → 2단 |
| D-23 | 재착륙 | 실제 팔콘 헤비와 동일. 측면 부스터 2기는 발사장 근처 착륙장, 중앙 1단은 바다 위 무인선. 2단은 회수하지 않음 |
| D-24 | 카메라 | 주 화면은 우주선(2단 이후)을 따라가고, 착륙 장면은 작은 보조 화면(화면 분할)으로 표시 |
| D-25 | 장면 연결 | "발사" 버튼 → 3D 발사 장면(1배~10배 배속, 건너뛰기 가능) → 2단 분리 후 우주선 출발 → 스톱워치·결과 화면으로 전환 |

### 만드는 것

3D 장면 코드는 새 폴더 `src/launch/`에 두고, 발사 물리 계산은 기존 원칙대로 `src/physics/`에, 로켓 제원은 `src/data/`에 둔다.

| 폴더 | 파일 | 내용 |
|---|---|---|
| `lib/three/` | `three.module.js` (동봉) | Three.js 라이브러리 원본. 수정하지 않음 |
| `src/data/` | `falconHeavy.js` | 각 단의 건조 질량, 추진제 질량, 추력, 비추력, 연소 시간. 출처 주석 |
| `src/data/` | `landingSites.js` | 발사장별 착륙장(부스터)과 무인선(코어) 위치 |
| `src/physics/` | `launchDynamics.js` | 추력·중력·질량 변화를 반영한 운동 방정식 적분 (순수 함수) |
| `src/physics/` | `staging.js` | 단 분리 조건(추진제 소진)과 분리 후 각 부분의 초기 상태 |
| `src/physics/` | `landingGuidance.js` | 부스트백, 낙하, 착륙 연소 시작 고도 계산 |
| `src/launch/` | `launchScene.js` | Three.js 장면 생성: 지구, 하늘, 발사대, 조명 |
| `src/launch/` | `rocketModel.js` | 로켓 4개 부분(부스터 2, 코어, 2단)의 3D 형상과 화염 표현 |
| `src/launch/` | `followCamera.js` | 대상 뒤에서 따라가는 3인칭 카메라 |
| `src/launch/` | `pipView.js` | 착륙 장면용 작은 보조 화면(picture-in-picture) 렌더링 |
| `src/launch/` | `launchTimeline.js` | 발사 → 부스터 분리 → 코어 분리 → 2단 분리 → 착륙 완료의 진행 상태 관리 |
| `src/launch/` | `launchHud.js` | 화면 위 정보 표시: 경과 시간, 고도, 속도, 현재 단계, 배속 조절, 건너뛰기 버튼 |
| `src/ui/` | `launchButton.js` | "발사" 버튼과 결과 화면으로의 전환 |

### 물리/계산 근거

[03_physics.md](03_physics.md) 6절에 정리했다. 요지는 다음과 같다.

- 운동 방정식: m(t)·dv/dt = T(방향) − m(t)·g(r), g(r) = GM/r²
- 질량 변화: dm/dt = −T / (Isp·g₀)
- 검증: 치올콥스키 로켓 방정식 Δv = Isp·g₀·ln(m₀/m_f)
- 착륙 연소 시작 고도: h = v² / (2·(a − g)) (일정 감속 가정)
- 시간 적분: 고정 시간 간격의 반음해 오일러(semi-implicit Euler) 또는 RK4. 배속은 한 프레임에 여러 번 적분하여 구현

**중요한 한계**: 2단 분리 직후 우주선의 실제 속도는 약 10 km/s 수준이다. 사용자가 슬라이더로 고른 속도(최대 0.99c)와는 맞지 않으므로,
발사 장면이 끝나는 시점에 "우주선이 지정 속도로 항해를 시작한다"고 보고 등속 모델([03_physics.md](03_physics.md) 2절)로 넘어간다.
발사 장면의 물리와 시간 지연 계산의 물리는 서로 독립이다.

### 단계 분할과 완료 조건

[04_roadmap.md](04_roadmap.md)에 10~15단계로 추가했다. 진행 순서는 1~7단계 → 10~15단계 → 8단계 → 9단계이다. (D-28)

| 단계 | 이름 | 완료 조건 |
|---|---|---|
| 10 | Three.js 도입과 빈 3D 장면 | 지구(구), 발사대, 조명이 보이고 카메라를 마우스로 돌릴 수 있다 |
| 11 | 로켓 모델과 발사 물리 (단 분리 없음, 1단 통짜) | 발사 버튼을 누르면 로켓이 물리 계산에 따라 상승하고 카메라가 따라간다. 배속 1~10배 |
| 12 | 단 분리 (부스터 → 코어 → 2단) | 추진제 소진 시점에 차례로 분리되고, 분리된 부분이 관성으로 계속 움직인다 |
| 13 | 재착륙 | 부스터 2기는 착륙장에, 코어는 무인선에 착륙 연소로 내려앉는다 |
| 14 | 화면 분할 카메라와 HUD | 주 화면은 2단, 보조 화면은 착륙 장면. 경과 시간·고도·속도·단계 표시, 건너뛰기 |
| 15 | 결과 화면 연결 | 2단 분리 후 출발 → 기존 스톱워치·결과 화면으로 자연스럽게 전환 |

### 미결 질문

2026-09-05에 모두 답변되어 [06_decisions.md](06_decisions.md) 3절의 D-39 ~ D-45로 확정되었다.
축척(표시 축척), 피치 프로그램(10초 후 2분간), 착륙 실패 연출 없음, 보조 화면 1개 자동 전환, 탑재체 10 t, 가상 착륙장, Three.js 최신 안정판.

### 다른 문서에 반영한 곳

- [01_requirements.md](01_requirements.md) 2.10절 추가, 2.1절 라이브러리 항목 수정
- [02_architecture.md](02_architecture.md) 폴더 구조에 `lib/`, `src/launch/` 추가, 5절 스크립트 로딩 갱신
- [03_physics.md](03_physics.md) 6절 발사 물리 추가
- [04_roadmap.md](04_roadmap.md) 10~15단계 추가
- [05_data_reference.md](05_data_reference.md) 6절 팔콘 헤비 제원, 7절 착륙장 추가
- [06_decisions.md](06_decisions.md) D-17 수정, D-19~D-25 추가, Q-11~Q-16 추가
- 새 폴더 README: [../src/launch/README.md](../src/launch/README.md), [../lib/README.md](../lib/README.md)
